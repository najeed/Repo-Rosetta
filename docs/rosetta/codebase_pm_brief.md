# CODEBASE.md - Repo Rosetta

## Overview
This is a high-fidelity architectural guide generated by Repo Rosetta.

## Configuration
- **Generated on**: 2026-03-16 23:47:13
- **Persona**: pm
- **Verbosity**: brief

## Architecture Map
```mermaid
graph TD
    classDef module fill:#f9f,stroke:#333,stroke-width:2px;
    subgraph root [root]
        "dump_tree.py"["dump_tree.py"]:::module
        "read_log.py"["read_log.py"]:::module
        "repro_ts.py"["repro_ts.py"]:::module
    end
    subgraph backend [backend]
        "backend/instances.py"["instances.py"]:::module
        "backend/main.py"["main.py"]:::module
        "backend/models.py"["models.py"]:::module
        "backend/__init__.py"["__init__.py"]:::module
    end
    subgraph backend_api [backend/api]
        "backend/api/chat.py"["chat.py"]:::module
        "backend/api/lsp_proxy.py"["lsp_proxy.py"]:::module
        "backend/api/router.py"["router.py"]:::module
    end
    subgraph backend_auth [backend/auth]
        "backend/auth/github.py"["github.py"]:::module
    end
    subgraph backend_db [backend/db]
        "backend/db/annotations.py"["annotations.py"]:::module
        "backend/db/audit.py"["audit.py"]:::module
        "backend/db/schema.py"["schema.py"]:::module
        "backend/db/session.py"["session.py"]:::module
    end
    subgraph backend_ecosystem [backend/ecosystem]
        "backend/ecosystem/diff_engine.py"["diff_engine.py"]:::module
    end
    subgraph backend_graph [backend/graph]
        "backend/graph/manager.py"["manager.py"]:::module
    end
    subgraph backend_intelligence [backend/intelligence]
        "backend/intelligence/connectors.py"["connectors.py"]:::module
    end
    subgraph backend_parser [backend/parser]
        "backend/parser/engine.py"["engine.py"]:::module
        "backend/parser/__init__.py"["__init__.py"]:::module
    end
    subgraph backend_summarizer [backend/summarizer]
        "backend/summarizer/engine.py"["engine.py"]:::module
        "backend/summarizer/quality.py"["quality.py"]:::module
    end
    subgraph backend_tests [backend/tests]
        "backend/tests/conftest.py"["conftest.py"]:::module
        "backend/tests/test_api.py"["test_api.py"]:::module
        "backend/tests/test_diff_engine.py"["test_diff_engine.py"]:::module
        "backend/tests/test_graph_semantic.py"["test_graph_semantic.py"]:::module
        "backend/tests/test_parser.py"["test_parser.py"]:::module
        "backend/tests/test_persistence.py"["test_persistence.py"]:::module
        "backend/tests/test_phase9_final.py"["test_phase9_final.py"]:::module
        "backend/tests/test_summarizer_llm.py"["test_summarizer_llm.py"]:::module
    end
    subgraph backend_utils [backend/utils]
        "backend/utils/logger.py"["logger.py"]:::module
    end
    subgraph cli [cli]
        "cli/rosetta.py"["rosetta.py"]:::module
    end
    subgraph frontend [frontend]
        "frontend/next-env.d.ts"["next-env.d.ts"]:::module
        "frontend/next.config.ts"["next.config.ts"]:::module
    end
    subgraph frontend_src_app [frontend/src/app]
        "frontend/src/app/layout.tsx"["layout.tsx"]:::module
        "frontend/src/app/page.tsx"["page.tsx"]:::module
    end
    subgraph frontend_src_components [frontend/src/components]
        "frontend/src/components/ArchitectureMap.tsx"["ArchitectureMap.tsx"]:::module
        "frontend/src/components/AuthGate.tsx"["AuthGate.tsx"]:::module
        "frontend/src/components/ChatOverlay.tsx"["ChatOverlay.tsx"]:::module
        "frontend/src/components/CollaborationPresence.tsx"["CollaborationPresence.tsx"]:::module
        "frontend/src/components/ExplanationPanel.tsx"["ExplanationPanel.tsx"]:::module
    end
    "backend/instances.py" --> "backend/graph/manager.py"
    "backend/instances.py" --> "backend/summarizer/engine.py"
    "backend/api/chat.py" --> "backend/summarizer/engine.py"
    "backend/api/chat.py" --> "backend/graph/manager.py"
    "backend/api/chat.py" --> "backend/instances.py"
    "backend/api/lsp_proxy.py" --> "backend/graph/manager.py"
    "backend/api/lsp_proxy.py" --> "backend/instances.py"
    "backend/api/router.py" --> "backend/models.py"
    "backend/api/router.py" --> "backend/db/annotations.py"
    "backend/api/router.py" --> "backend/db/audit.py"
    "backend/api/router.py" --> "backend/graph/manager.py"
    "backend/api/router.py" --> "backend/parser/engine.py"
    "backend/api/router.py" --> "backend/auth/github.py"
    "backend/api/router.py" --> "backend/summarizer/engine.py"
    "backend/api/router.py" --> "backend/instances.py"
    "backend/db/annotations.py" --> "backend/db/session.py"
    "backend/db/annotations.py" --> "backend/db/schema.py"
    "backend/db/audit.py" --> "backend/db/session.py"
    "backend/db/audit.py" --> "backend/db/schema.py"
    "backend/db/schema.py" --> "backend/db/session.py"
    "backend/graph/manager.py" --> "backend/models.py"
    "backend/summarizer/engine.py" --> "backend/intelligence/connectors.py"
    "backend/summarizer/engine.py" --> "backend/utils/logger.py"
    "backend/summarizer/engine.py" --> "backend/summarizer/quality.py"
    "backend/tests/conftest.py" --> "backend/auth/github.py"
    "backend/tests/conftest.py" --> "backend/main.py"
    "backend/tests/test_diff_engine.py" --> "backend/ecosystem/diff_engine.py"
    "backend/tests/test_graph_semantic.py" --> "backend/graph/manager.py"
    "backend/tests/test_graph_semantic.py" --> "backend/models.py"
    "backend/tests/test_parser.py" --> "backend/parser/engine.py"
    "backend/tests/test_persistence.py" --> "backend/db/annotations.py"
    "backend/tests/test_persistence.py" --> "backend/db/session.py"
    "backend/tests/test_persistence.py" --> "backend/db/schema.py"
    "backend/tests/test_persistence.py" --> "backend/db/audit.py"
    "backend/tests/test_phase9_final.py" --> "backend/api/router.py"
    "backend/tests/test_summarizer_llm.py" --> "backend/summarizer/engine.py"
    "cli/rosetta.py" --> "backend/utils/logger.py"
    "cli/rosetta.py" --> "backend/graph/manager.py"
    "cli/rosetta.py" --> "backend/models.py"
    "cli/rosetta.py" --> "backend/summarizer/engine.py"
    "cli/rosetta.py" --> "backend/parser/engine.py"
```

## Module Reference
| Module | Description |
|---|---|
| dump_tree.py | This Python module, `dump_tree.py`, provides functionality for parsing and visualizing code syntax trees.

**Key Components:**

*   **Dependencies:** It imports `tree_sitter` and `tree_sitter_python`, indicating it relies on these libraries for abstract syntax tree (AST) manipulation.
*   **Core Logic:**
    *   The `dump_tree` function likely handles the core logic of processing and outputting the syntax tree representation.
    *   The `main` function serves as the entry point, suggesting it orchestrates the parsing and dumping process, possibly for a given input file or code snippet.

**Business Impact:**

This module is critical for code analysis, refactoring tools, and static analysis engines that require a deep understanding of code structure. Its ability to represent and traverse ASTs directly supports features like code linting, syntax highlighting, and automated code generation. |
| read_log.py | The `read_log.py` module is responsible for processing log data. It imports the `os` module, indicating potential interaction with the operating system or file system. The core functionality is encapsulated in the `read_log_chunks` function, which suggests a capability for reading and handling log entries in segmented or chunked formats. This module's primary impact lies in its ability to ingest and parse log information, forming a foundational component for any logging analysis or monitoring system. |
| repro_ts.py | The `repro_ts.py` module primarily serves as an entry point for demonstrating Tree-sitter functionality within a Python environment. It directly imports the `tree_sitter` and `tree_sitter_python` libraries, indicating a dependency on these core parsing components. The module's main executable logic is encapsulated within the `debug_versions` function, which is designed to showcase or debug the interaction between these Tree-sitter bindings. This module's direct business impact would be in enabling development and testing workflows for projects that leverage Tree-sitter for code analysis or manipulation. |
| backend/instances.py | This Python module, `backend/instances.py`, is responsible for managing instances. It imports functionality from `backend.graph.manager` and `backend.summarizer.engine`. Its external dependencies indicate it influences at least two other modules and is directly called by three. This suggests it plays a central role in instance-related operations within the backend, likely involving graph manipulation and summarization processes. |
| backend/main.py | The main module 'backend/main.py' is a type of 'module'. It resides in the path 'backend/main.py', and its code lies between lines 1 to 19. The module contains several import statements such as 'os', 'backend.api', 'dotenv', and 'fastapi'. It also has one main function named 'root' which is currently not providing any out-degree (i.e., no other modules or functions it calls on) and in-degree of 1 suggesting that this module is being called by at least another code entity. This specific module seems to primarily focus on setting up the backend environment, possibly through configuration settings or API setup.
 |
| backend/models.py | The 'backend/models.py' module, which is a Python file in the backend directory, has metadata about its structural attributes and identifiers. This module imports pydantic, typing modules for specific functionalities. It also contains two classes - CodeEntity and RepositoryContext, each having their own functionalities but not yet defined in the given codebase. 

The out_degree of this file is zero, indicating no outward dependencies on other files or functions within the same module. The in_degree indicates that it has 4 dependencies, which are assumed to be methods within classes from other modules. The code doesn't define these classes but they might include functionalities like data validation and manipulation in a database-like environment.
 |
| backend/__init__.py | The 'backend/__init__.py' module is a Python file located in the backend directory. It appears to be an initialisation file for the backend package and does not directly contain any functional code or logic. Its purpose seems to be primarily structuring the backend package, with no apparent direct impact on business operations. As such, it doesn't have any dependencies outside of its own module, and has zero inbound connections (indicating there are no modules that depend on this one). The lines of code range from 1 to 2, but as per your query, we don't know the content of these lines since 'summary' field is None.
 |
| backend/api/chat.py | The Python module 'backend/api/chat.py' is acting as a backend API for chat functionalities. It imports various libraries and modules such as the summarizer engine, typing, graph manager, pydantic (for data validation), instances (possibly related to some sort of application configurations or instances), fastapi (an ASGI framework for building APIs), and classes like ChatRequest and ChatResponse.

The module 'backend/api/chat.py' contains one main function called 'chat_with_codebase'. This function is likely responsible for handling chat interactions with the codebase, possibly involving some form of natural language processing or machine learning to understand and respond to user inputs. The metadata also indicates that this module has no dependencies on other modules (in degree = 0), but it seems to be relying heavily on external libraries and classes for its functionality.
 |
| backend/api/lsp_proxy.py | This Python module, 'backend/api/lsp_proxy.py', is a backend API responsible for handling Language Server Protocol (LSP) requests and responses. It is structured in a way that includes several external dependencies such as FastAPI, the graph manager from 'backend.graph.manager', instances from 'backend.instances', Pydantic, and a custom LSPRequest class. 

The module contains six major entities: two Python standard libraries (FastAPI and Pydantic), one function (lsp_proxy), and one class (LSPRequest). The main functionalities include the processing of LSP requests by leveraging FastAPI for API setup, managing graph instances with 'backend.graph.manager', and using Pydantic to handle data validation. 

This module doesn't appear to have any direct business logic since its purpose isn't explicitly stated in the metadata. However, it depends heavily on these external libraries which could potentially serve as a foundation for other parts of the application or be independently useful within their respective domains. The number of outgoing dependencies ('out_degree') suggests that this module has potential reusability and may contribute to codebase's more modularity and efficiency.
 |
| backend/api/router.py | This is a Python module named `backend/api/router.py` that contains several dependencies and functions for handling annotations, including imports of various modules like `os`, `typing`, etc., as well as class definitions (`AnnotationRequest`), function calls such as `get_graph()`, `save_annotation()`, etc. It appears to be a key component in the backend system, likely responsible for managing and handling annotations related operations. The module has dependencies on other modules like database management ('annotations', 'audit'), graph processing ('manager'), parsing and summarizing functionalities ('engine' or 'summarizer'), authentication via GitHub, and data validation using Pydantic models, all of which add to the complexity and functionality of this code entity.
 |
| backend/auth/github.py | This Python module 'backend/auth/github.py' contains several entities and functions which are used for GitHub authentication in a software system. It imports external dependencies such as 'typing', 'httpx', and 'os'. The main class, 'GitHubAuth', is defined with three methods: '__init__', 'get_access_token', and 'verify_permission'. This module seems to be involved in the authentication flow of a software system where it gets an access token from GitHub's API using the httpx library. The 'verify_permission' function is used for checking if the user has necessary permissions.
 |
| backend/db/annotations.py | The 'backend/db/annotations.py' module is a Python script that contains the definition of several classes and functions related to database annotations. The file begins at line 1 and ends at line 60. This module has direct dependencies on external modules such as os, json, backend.db.session, sqlalchemy.orm, backend.db.schema, and typing.

The main component of this module is the 'AnnotationManager' class which appears to handle operations related to database annotations like saving ('save_annotation') and retrieving ('get_annotations', 'get_all_annotations'). These functions are likely part of a broader system that deals with data management, possibly involving CRUD (Create, Read, Update, Delete) operations on the annotations.

The module's impact in the overall application is likely to be significant as it manages database interactions and contains the main business logic for handling these annotations. However, without additional context or specific business requirements, we cannot definitively say what this module directly impacts in terms of user experience, performance, error rates, etc.
 |
| backend/db/audit.py | The file 'backend/db/audit.py' represents a module of type 'module'. It is located at the specified path and covers lines from 1 to 37. The main objective of this module is to provide logging functionalities for system accesses, using external imports including os, typing, datetime, and custom class AuditLogger. It has dependencies on other modules such as backend.db.session and backend.db.schema. This module interacts with another two modules, indicating an out-degree of 2 (dependencies) and in_degree of 2 (other modules depending on this one). The metadata suggests that the purpose of this code entity is to monitor system accesses and log them for future reference or analysis.
 |
| backend/db/schema.py | This is a Python module named 'backend/db/schema.py'. It resides at the path mentioned and spans from line 1 to 23 in the file. The module imports SQLAlchemy, datetime, backend.db.session which might be used for database operations. This module defines two classes: Annotation and AuditLogEntry that likely represent business logic depending on their use case. It has a high degree of interconnectedness with other modules indicating it's part of the overall system architecture.
 |
| backend/db/session.py | The file 'backend/db/session.py' is a module that contains metadata related to database sessions. It imports the necessary SQLAlchemy libraries for interacting with the database ('sqlalchemy', 'sqlalchemy.orm') and the OS library for environment variable manipulation. The purpose of this module seems to be managing database sessions by providing a function 'get_db' that creates and configures these sessions.
 |
| backend/ecosystem/diff_engine.py | This Python module 'backend/ecosystem/diff_engine.py' contains a class named `GraphDiffEngine` with two methods - `compare_graphs()` and `get_regression_metadata()`. The file is using the built-in `typing` module, which means it relies on type hinting to define function signatures and variable types.

The `compare_graphs()` function appears to be a part of graph comparison logic, while `get_regression_metadata()` seems to handle metadata extraction related to regression testing. The file is not showing any business-related logic as it only contains code for data processing and difference calculation, which might be used in machine learning applications like anomaly detection or predictive maintenance.

This module does not depend on other modules because its dependencies are purely internal (builtin `typing`). It appears to have no direct impact on the overall business logic of the system as it is more related to data processing and calculation rather than any significant application logic that could affect the overall operation of an end-user facing web or mobile app.
 |
| backend/graph/manager.py | This code entity represents a Python module named 'backend/graph/manager.py'. It resides in the backend directory and contains various functionalities that interact with graph-related data structures such as classes (GraphManager), functions (__init__, add_relationship, build_semantic_connections, add_entity, find_relevant_nodes, get_node_context, get_execution_flow, get_graph_data). The module imports various libraries like 'typing', 'backend.models' and has an average out-degree of 1, in-degree of 6 indicating the dependencies between its functions.
 |
| backend/intelligence/connectors.py | This is a Python module named 'connectors.py' in the 'intelligence' directory of the 'backend'. It contains several key entities such as classes and functions which are related to data processing and knowledge extraction. This module appears to be responsible for connecting to an external system or database, analyzing data, and providing insights via its methods like `get_related_context` and `get_suggestions`.
 |
| backend/parser/engine.py | This module named 'backend/parser/engine.py' is a Python module that contains several entities of different types and functions. This includes imports of external libraries such as os, shutil, typing, and tree_sitter; an initialization function __init__, a parser engine class ParserEngine, a cleanup function named cleanup, and three other functions: _extract_entities, parse_file, and _get_lang. 

The module appears to be responsible for parsing files using the tree_sitter library. The main class ParserEngine is initialized in __init__ but it doesn't seem like this initialization is used elsewhere in the codebase, which indicates that its usage is localized or specific to this module.

This module may have dependencies on other modules and libraries such as 'os', 'shutil', 'typing', and 'tree_sitter'. The use of _extract_entities, parse_file, and _get_lang functions could suggest these are the main functionalities offered by this module. However, without more context or additional information about how these functions interact with each other, it's difficult to provide a complete understanding of their business logic. 

As for impact, this module appears to be providing an interface between external libraries and the rest of the system by parsing files using tree_sitter, possibly for syntax-based analysis or code extraction tasks in larger systems. It depends on these external libraries for its functionality but does not seem to have direct business logic related to other modules or services within the system.
 |
| backend/parser/__init__.py | This is a Python module named '__init__.py', located in the 'parser' directory of the 'backend' project. It appears to be an empty module as indicated by its metadata indicating no contained entities or dependencies, and outgoing edges (dependencies on other modules). The purpose of this module might be related to parsing functionality within the backend system.
 |

Generated by Repo Rosetta.
